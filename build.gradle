allprojects {
  group = "io.kfec"
  version = "1.0"

  repositories {
    jcenter()
  }
}

subprojects {
  version = project.version
}

task clean {
  dependsOn subprojects.collect { it.tasks.matching { it.name == 'clean' } }
}
